read = int(input())

largest = 0

while read > -1:
    if read > largest:
        largest=int(read)
    increment = int(input())  # Get increment value from user input
    read = increment
    if read <= 0:
        break

print(largest)
